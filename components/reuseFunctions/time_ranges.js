import React from 'react'

function time_ranges(StartDate1, EndDate1, StartDate2, EndDate2) {

  if ((StartDate1 <= EndDate2) && (StartDate2 <= EndDate1)){
  console.log('-----   NotOverlapped -----  ')
  }
  // return (
  //   <div>daily_Time</div>
  // )
}

export default time_ranges